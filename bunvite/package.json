{
  "name": "bunvite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --hot server.js",
    "build": "vite build && vite build --ssr",
    "preview": "bun server.js"
  },
  "devDependencies": {
    "vite": "^7.0.4"
  }
}
